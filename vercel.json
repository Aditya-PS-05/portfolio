{
  "buildCommand": "bundle exec jekyll build",
  "outputDirectory": "_site",
  "installCommand": "bundle config set --local deployment 'true' && bundle install",
  "framework": null,
  "devCommand": "bundle exec jekyll serve --livereload",
  "env": {
    "JEKYLL_ENV": "production"
  }
}